prometheus:
  enabled: false
  prometheusSpec:
    retention: 3d
    externalUrl: "http://prometheus.system.cloudzen.me"
    routePrefix: /
    resources:
      requests:
        cpu: 200m
        memory: 512Mi
      limits:
        cpu: 500m
        memory: 1Gi

grafana:
  enabled: true
  adminPassword: "changeme"
  grafana.ini:
    server:
      root_url: "http://grafana.system.cloudzen.me"

alertmanager:
  enabled: false

nodeExporter:
  enabled: true

kubeStateMetrics:
  enabled: true
